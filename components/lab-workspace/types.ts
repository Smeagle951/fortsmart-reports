/** JSON exportado pelo app FortSmart Lab (SQLite → partilha). */
export type LabExportEnvelope = {
  id?: string;
  created_at?: string;
  source_text?: string | null;
  engine_payload?: Record<string, unknown> | null;
};
